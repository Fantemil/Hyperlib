local script = -- put whatever version here, use setclipboard(tostring(game.PlaceVersion))
local gamee = game.PlaceVersion

if script == gamee then
     print("up to date, loading")
else
     print("Game has updated")
end